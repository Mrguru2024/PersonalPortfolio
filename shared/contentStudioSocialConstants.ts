/**
 * Max OAuth (or stored) accounts per platform for Content Studio Integrations.
 * Raised for multi-brand ops: multiple Pages / LinkedIn profiles / company pages on one deployment.
 */
export const MAX_SOCIAL_CONNECTIONS_PER_PLATFORM = 16;
