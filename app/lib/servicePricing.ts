/**
 * Market-aligned illustrative ranges (US SMB / growth-agency positioning, 2026).
 * Final investment is always scoped in writing — these guide expectations on-site and in SEO/meta.
 */

export const STARTUP_GROWTH_AUDIT_PRICE_RANGE = "$497 – $899";

/** Compact form for meta descriptions (no spaces around em dash). */
export const STARTUP_GROWTH_AUDIT_PRICE_META = "$497–$899";
